build.arch=macosx-x86
swt.jar=swt-3.7.1-macosx-x86.jar

dist.file=${build.dir}/spritey-${app.ver}-${build.arch}.zip

exe.name=${app.name}.app/
exe.file=${build.dir}/${exe.name}

java.root.dir=${exe.file}/Contents/Resources/Java